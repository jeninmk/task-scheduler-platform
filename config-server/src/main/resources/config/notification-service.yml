# Local development configuration for notification-service
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Eureka disabled for local development
eureka:
  client:
    enabled: false
